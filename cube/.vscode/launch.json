{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ejecutar Cubo",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main", // Ruta al ejecutable
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Habilitar la salida de la consola",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Compilar Cubo", // Tarea de compilación
            "miDebuggerPath": "/usr/bin/gdb", // Ruta al depurador
            "setupCommands": [
                {
                    "description": "Habilitar la salida de la consola",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}
